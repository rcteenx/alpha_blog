<% if @article.errors.any? #ilk çalışmada yok bu nesne %>
  <h2>The following errors prevent the article from being saved.</h2>
  <ul>
  <% @article.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
<% end %>